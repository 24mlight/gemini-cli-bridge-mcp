[project]
name = "gemini-mcp-python"
version = "0.1.0"
description = "Python MCP server wrapper for Gemini CLI (fallback to multiple install locations)"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "mcp[cli]>=1.22.0",
]

[tool.uv]
dev-dependencies = []

